services:
  checker:
    image: ghcr.io/zfb132/manuscript-monitor:latest
    build:
      context: .
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
      ulimits:
        nofile:
          soft: 65536
          hard: 65536
    init: true
    # user: ${UID}:${GID}
    restart: unless-stopped
    networks:
      - manuscript-monitor-net
    env_file:
      - .env
    environment:
      CHROME_BIN: /usr/bin/chromium
      CHROMEDRIVER_PATH: /usr/bin/chromedriver
      RUNNING_IN_DOCKER: "1"
      CRON_SCHEDULE: ${CRON_SCHEDULE:?Set CRON_SCHEDULE in .env}
      TZ: ${TZ:-UTC}
    volumes:
      - type: bind
        source: ./config.toml
        target: /app/config.toml
        read_only: true
      - type: bind
        source: ./data
        target: /app/data
    shm_size: "1gb"
    command:
      - /bin/sh
      - -euc
      - |
        printf '%s %s\n' \
          "$${CRON_SCHEDULE}" \
          '/app/.venv/bin/python /app/main.py --config /app/config.toml' \
          > /tmp/manuscript-monitor.crontab
        supercronic -test /tmp/manuscript-monitor.crontab
        /app/.venv/bin/python /app/main.py --config /app/config.toml \
          || printf '%s\n' \
             'Initial check failed; scheduled checks will continue.' >&2
        exec supercronic \
          -split-logs \
          /tmp/manuscript-monitor.crontab

networks:
  manuscript-monitor-net:
    driver: bridge

# use `mkdir data` to create the data directory before running `docker compose up`
